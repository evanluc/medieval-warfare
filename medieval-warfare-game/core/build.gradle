apply plugin: "java"

[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets.main.java.srcDirs = [ "src/" ]

dependencies {
    compile project(':medieval-warfare-client')
}

eclipse.project {
    name = "medieval-warfare-game-core"
}
